server {
    listen 8080;
    server_name _;

    root /opt/app-root/src;
    index index.html;

    location / {
        try_files $uri /index.html;
    }

    location = /config.js {
        add_header Cache-Control "no-store";
        try_files $uri =404;
    }
}
